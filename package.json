{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "dev": "run-p privcy:dev format:watch",
    "dev-docs": "run-p docs:dev format:watch",
    "build": "run-s privcy:build",
    "build-docs": "run-s docs:build",
    "privcy:dev": "pnpm --filter privcy dev",
    "privcy:build": "pnpm --filter privcy build",
    "docs:dev": "pnpm --filter docs dev",
    "docs:build": "pnpm --filter docs build",
    "format": "prettier --write",
    "format:watch": "onchange \"{packages/privcy,docs}/**/*.{ts,tsx,mts,js,jsx,css,scss,html,svelte}\" --exclude-path .gitignore -- prettier --write --ignore-unknown {{changed}}"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "onchange": "^7.1.0",
    "prettier": "^3.5.1"
  },
  "packageManager": "pnpm@10.4.0"
}
